# Thin shim for `pip install -r requirements.txt` muscle memory.
# The real dependency specification lives in pyproject.toml;
# constraints.txt pins the fully verified environment.
--extra-index-url https://download.pytorch.org/whl/cpu
-e .
